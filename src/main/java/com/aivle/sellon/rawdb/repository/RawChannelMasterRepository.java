package com.aivle.sellon.rawdb.repository;

import com.aivle.sellon.rawdb.entity.RawChannelMaster;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RawChannelMasterRepository extends JpaRepository<RawChannelMaster, String> {
}
